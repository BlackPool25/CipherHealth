# Decent-Hospital Backend Dependencies
# Install with: pip install -r requirements.txt

# Core framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Cryptography (pyumbral is optional - we use fallback if not available)
cryptography>=41.0.0
# Note: pyumbral may not be available on all platforms
# Install manually if needed: pip install umbral

# JWT Authentication
python-jose[cryptography]>=3.3.0

# HTTP client
requests>=2.31.0
httpx>=0.25.0

# Environment & Config
python-dotenv>=1.0.0
pydantic>=2.5.0

# Ethereum/Web3
web3>=6.11.0
eth-account>=0.10.0

# Database
aiosqlite>=0.19.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
